protocol_version: "0.1"
project_id: gestionpisos
display_name: "GestionPisos / Allaiso"
integration_status: "AUTH_READONLY_REUSE_IMPLEMENTED_UNVERIFIED"

allowed_environments:
  test:
    base_url: "https://jdlc86.github.io/gestionpisos/"

required_capabilities:
  - browser
  - aria_snapshot
  - screenshot

secret_names: []

safety:
  production_allowed: false
  destructive_tests_allowed: false
  authentication_allowed: true
  authentication_mode: "isolated_session_reuse_read_only"
  credential_entry_allowed: false
  mfa_automation_allowed: false
  allowed_url_prefixes:
    - "https://jdlc86.github.io/gestionpisos/"

notes:
  - "Automatic OpenClaw Gateway bootstrap verified on 2026-09-23."
  - "GestionPisos Public Smoke #15 passed in GitHub Actions run 35806072819 on commit 3db8b882afc45d631204e133fc5a796c7ce920d3."
  - "QA Node Handshake #16 passed in GitHub Actions run 35806072832 on the same commit."
  - "Public smoke evidence includes a real Chrome screenshot of the Allaiso access screen."
  - "Authenticated QA is limited to the isolated qa-gestionpisos-auth profile and reusable session inspection."
  - "No password, TOTP, token, cookie, storage export, form submission, or AUT mutation is allowed by this stage."
  - "Login, MFA challenge, or MFA setup are classified as BLOCKED rather than as an Allaiso functional failure."
  - "The workflow battery in the GestionPisos repository remains the source of truth for product PASS/FAIL."
